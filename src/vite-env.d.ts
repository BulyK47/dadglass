/// <reference types="vite/client" />

/** Injected by Vite: package.json version plus the Android versionCode — "1.0.6 (7)" —
 *  or the bare version on a tree with no android/ project. See `define` in vite.config.ts. */
declare const __APP_VERSION__: string;
